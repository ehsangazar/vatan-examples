[project]
name = "vatan-genai-batch-file"
version = "1.0.0"
description = "The whole Files lifecycle on Vatan with the genai SDK"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.0.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
]

[tool.uv]
package = false
