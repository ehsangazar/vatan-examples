[project]
name = "vatan-genai-embeddings"
version = "1.0.0"
description = "A embeddings example for Vatan with Google's genai SDK"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
