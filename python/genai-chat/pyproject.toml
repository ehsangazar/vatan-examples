[project]
name = "vatan-genai-chat"
version = "1.0.0"
description = "A chat example for Vatan with Google's genai SDK"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.0.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
package = false
