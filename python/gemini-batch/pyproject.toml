[project]
name = "vatan-gemini-batch"
version = "1.0.0"
description = "Run a Gemini batch on Vatan with Google's own genai SDK"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=2.0.0",
    "python-dotenv>=1.0.0",
    "httpx>=0.27.0",
]

[tool.uv]
package = false
